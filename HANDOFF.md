# บริบทตั้งต้น — ปฐมเทพ (genesis)

อ่านไฟล์นี้ครั้งเดียวตอนเซสชันแรก กติกาประจำวันอยู่ใน `CLAUDE.md` แล้ว

## คนที่คุณกำลังทำงานด้วย

Tap — นักศึกษาปริญญาเอกที่ Sheffield ทำ agent-based modelling ของโครงข่ายรถไฟด้วย FLAMEGPU2
แข็ง Python กำลังสร้างความคล่อง C++/CUDA **ไม่ได้มาสายเขียนโค้ดแอป** TypeScript เป็นของใหม่

วิธีทำงานที่เขาขอไว้:
- ตอบภาษาไทย
- อธิบายโค้ดเป็นขั้นตอน ใช้เกมนี้เป็นเครื่องมือเรียนรู้ไปด้วย
- พูดตรง เห็นตรรกะพัง เห็นโค้ดผิด เห็นความเสี่ยง บอกเลย ไม่ต้องประคอง
- อ้างอิงต้องจริง ไม่รู้ให้บอกว่าไม่รู้

นี่เป็นโปรเจกต์งานอดิเรก ทำแบบ vibe code ค่อยๆ เกลาไปเรื่อยๆ ให้ใหญ่ขึ้น
ไม่มีเดดไลน์ ไม่ต้องรีบ แต่ต้องไม่พังสะสม

## เครื่องและวิธีเล่น

พัฒนาบน MacBook Pro M3 Pro เล่นบน Mac เป็นหลัก และเปิดบน iPad ผ่าน Wi-Fi วงเดียวกัน
`server.host: true` ใน vite.config.ts คือบรรทัดที่ทำให้ iPad เปิดได้ **อย่าลบ**
สองโปรเจกต์ใช้คนละพอร์ตเพื่อให้รันพร้อมกันได้

## การตัดสินใจที่ล็อกแล้ว (ไม่ต้องรื้อใหม่)

**TypeScript + Vite + Canvas** — เลือกเพราะมันแก้โจทย์ "พัฒนาบน Mac เล่นบน iPad" ได้ทันที
โดยไม่ต้องมี Xcode ไม่ต้องจ่าย Apple Developer ไม่ต้อง build ใหม่ทุกครั้ง
Godot/Unity ถูกตัดออกด้วยเหตุผลนี้ ไม่ใช่เพราะมันแย่ ถ้าวันหนึ่งเกมต้องการ 3D
หรือ physics engine จริงจัง ค่อยคุยกันใหม่ — ตอนนั้นเหตุผลจะเปลี่ยนไปแล้ว

**หนึ่งโปรเจกต์ = หนึ่ง git repo** ไม่รวมเป็น monorepo

## เกมนี้คืออะไร

Tap เล่น Black & White มาเยอะและเสียดายที่มันจบแค่ภาค 2 นี่คือความพยายามสร้างเกมแบบนั้น
**เกมสามมิติ เกาะเดียว หมุนกล้องรอบได้** ทำด้วย Three.js บนโครง Vite เดิม

ลูปหลักมีอย่างเดียว:
> ผู้คนขาดอะไรสักอย่าง → ร้องขอ → ท่านร่ายปาฏิหาริย์ไปช่วย → ศรัทธาเพิ่ม → ร่ายคาถาใหญ่ขึ้นได้

บวกสัตว์เลี้ยงตัวเดียวที่สอนได้ด้วยการชม (✦) และลงโทษ (✕) มันจำได้ทั้ง "ทำอะไรดี"
และ "ที่ไหนดี" และแกนธรรม/อธรรมที่เปลี่ยนราคาคาถาทั้งสองฝ่าย

**15 ก.ย. 2026 ตัดขอบเขตลง** เพราะเวอร์ชันก่อนหน้ามีหกระบบพร้อมกันจนเล่นไม่รู้เรื่อง
ตัดออก: เทพคู่แข่ง · ประชากรสัตว์ป่าและการคัดเลือก · ระบบยุค 5 ยุค · ฤดูกาล
คาถาทั้ง 7 ใช้ได้ตั้งแต่นาทีแรก ศรัทธาคือด่านเดียวที่กั้นอยู่

## สถานะปัจจุบัน (ทดสอบด้วย `npm run sim 20 8`)

- เกาะ 24×24 เห็นทั้งใบในจอเดียว ลากหมุน หนีบซูม
- ภูมิประเทศ 3 มิติจาก `tile.h` เดิม · ต้นไม้และหินเป็น InstancedMesh · ทะเลกลืนกับหมอก
- หมู่บ้านสูงสุด 5-6 แห่งตามขนาดผืนดิน ประชากรรวม 30-250 ขึ้นกับว่าท่านดูแลแค่ไหน · มีป้ายลอยบอกว่าขาดอะไร
- แถบกลางจอบอกตลอดว่า "ตอนนี้ควรทำอะไร" และคาถาไหนช่วยได้
- **ชาวบ้านเดินให้เห็นจริง** สูงสุด 96 ตัว ออกไปทำกินแล้วแบกของกลับ ยืนเก้ๆ กังๆ ตอนขาดแคลน
  มายืนเป็นวงโค้งให้กลางหมู่บ้านหลังเห็นปาฏิหาริย์ และนั่งซมตอนโรคระบาด
- ไม่ล่มสลายสักโลก แกนธรรมไม่เลื่อนเอง · เซฟ/โหลดผ่านทุกเทสต์
- เปิดในเบราว์เซอร์แล้วไม่มี console error · ร่ายคาถาแล้วเห็นอนุภาคและผลกับดินจริง

**15 ก.ย. 2026 อุดช่องเทสต์** `npm run sim` เดิมเรียกแต่ `stepTick` ไม่เคยร่ายคาถาสักครั้ง
ลูปหลักของเกมจึงไม่มีอะไรคุมเลย ตอนนี้มีผู้เล่นจำลองที่ตอบคำขอของหมู่บ้านและสอนสัตว์จริง
เดินโลกเดียวกันสามแบบแล้วเทียบกัน: **ปล่อยทิ้ง ประชากรเหลือ 32 · เมตตา 232 · พิโรธ 5**
ใช้คาถาครบ 7/7 บรรทัดเปรียบเทียบนี้คือคำถามว่าการมีเทพเปลี่ยนอะไรได้จริงไหม

ระหว่างทางเจอบั๊กที่ผู้เล่นโดนเต็มๆ: `looksValid()` เช็ก `creatures` (พหูพจน์) ค้างไว้
จากตอนตัดขอบเขต พอเหลือสัตว์ตัวเดียวมันเลยตอบ false กับเซฟทุกไฟล์
**เกมเซฟลงได้แต่โหลดไม่ขึ้นสักช่องมาตลอด** แก้แล้วและเทสต์เดินผ่านด่านเดียวกับเกมจริงแล้ว

## แผนงาน — อ่านข้อนี้ก่อนจะเพิ่มฟีเจอร์อะไรก็ตาม

**ปัญหาที่ใหญ่กว่าฟีเจอร์: เกมนี้ยังไม่มีเป้าหมาย และแพ้ไม่ได้**
ไม่มีเงื่อนไขชนะ มีแต่ `s.dead` ตอนคนหมดเกาะ และ `npm run sim 20 8` ก็เพิ่งพิสูจน์ว่า
เทพเมตตาไม่มีโลกไหนล่มเลยสักโลก ประชากรจบที่ 232 ทุกครั้ง
ใส่ฟีเจอร์เพิ่มลงในเกมที่ไม่มีทั้งเป้าหมายและความเสี่ยง ไม่ทำให้มันสนุกขึ้น

เลือกทางเดียวก่อน อย่าทำสองทางพร้อมกัน — การทำหลายอย่างพร้อมกันคือสาเหตุที่ต้องตัดขอบเขตรอบที่แล้ว

**15 ก.ย. 2026 รอบสอง: ลุยทั้งสองทางพร้อมกัน** ข้อที่ขีดฆ่าคือทำแล้ว

### ทาง ก — ทำให้มันเป็น "สถานที่"

1. ~~**ชาวบ้านเป็นตัวๆ**~~ — **ทำแล้ว 15 ก.ย. 2026** (`src/render/villagers3d.ts`)
   วาดสูงสุด 96 ตัวด้วย InstancedMesh ตำแหน่งคำนวณสดจาก state ของหมู่บ้าน ไม่มี state ของตัวเอง
   มีสี่อารมณ์ตามสภาพหมู่บ้าน: ออกไปทำกินแล้วแบกของกลับ · ยืนเก้ๆ กังๆ ตอนขาดแคลน ·
   มายืนเป็นวงโค้งให้กลางหมู่บ้านหลังเห็นปาฏิหาริย์ · นั่งซมตอนโรคระบาด
2. ~~**ปลดล็อกกล้องให้ลงไปใกล้พื้น**~~ — **ทำแล้ว** ระยะต่ำสุด 14 → 4
   **แตะสองครั้งที่ช่องไหนก็ได้ = ร่อนลงไปดูตรงนั้น** จุดหมุนกล้องเลื่อนตามไปด้วย
   มีปุ่ม "ถอยออก" โผล่มาเองตอนอยู่ใกล้ และกล้องมุดใต้ดินไม่ได้แล้ว
3. ~~**มือของพระเจ้า 3 มิติ**~~ — **ทำแล้วบางส่วน** (`src/render/hand3d.ts`)
   มือลอยอยู่เหนือจุดที่ชี้ กำมือตอนเล็งคาถา เปลี่ยนสีตอนเล็งคาถาดำ
   และปุ่ม **"ยกไปวาง"** ยกสัตว์ไปวางที่อื่นได้จริง (`placeCreature`)
   **หยิบของและขว้างได้แล้ว** (`src/sim/physics.ts`) — แตะ "หยิบของ" แล้วแตะช่องที่มี
   ต้นไม้ · ก้อนหิน · หรือผืนดินอุดม แล้วแตะอีกช่องเพื่อขว้าง วิถีเป็นพาราโบลาจริง
   ตกน้ำก็แค่กระเพื่อม ตกใส่หมู่บ้านถึงจะมีผล
   - **ก้อนหิน** ทำประชากรลด ดินเสีย ธรรมลบ และสัตว์ที่อยู่ใกล้จำว่าตรงนั้นอันตราย
   - **ต้นไม้** ปลูกป่าที่ใหม่ ธรรมบวก
   - **อาหาร** เติมความต้องการอาหารให้หมู่บ้านโดยตรง

   **จุดสำคัญของระบบนี้: มือไม่กินศรัทธาสักหน่วย** ตอนศรัทธาหมดยังทำอะไรได้อยู่
   แค่ช้ากว่าและต้องเดินหาของเอง ผู้เล่นจำลองในเทสต์ใช้มือตอนศรัทธาไม่พอจริง
   และเทพเมตตาที่ใช้มือสลับกับคาถาถึงเป้าหมายบ่อยกว่าเดิม

   **ยังไม่ได้ทำ:** หยิบชาวบ้าน (ต้องให้ชาวบ้านมีตัวตนใน `src/sim/` ก่อน)
   ทรงมือยังหยาบ (ฝ่ามือทรงกลมแบน + นิ้วแคปซูล) พอดูออกว่าเป็นมือ แต่ยังไม่สวย
4. ~~**วัฏจักรกลางวัน/กลางคืน**~~ — **ทำแล้ว** หนึ่งวัน = `time.ticksPerDay` (200 tick)
   กลางคืนมีแสงจันทร์ (ไม่ใช่มืดสนิท) และหมู่บ้านติดกองไฟ ยิ่งคนเชื่อมากไฟยิ่งสว่าง

### ทาง ข — ทำให้มันเป็น "เกม"

1. ~~**วงอิทธิพล**~~ — **ทำแล้ว** (`influenceOf` / `inInfluence` ใน `village.ts`)
   ร่ายนอกเขตไม่ได้ เขตโตตามความเชื่อและจำนวนคน วาดเป็นวงจางบนพื้นให้เห็น
   ผู้เล่นจำลองใน `npm run sim` เคารพกติกาเดียวกัน และประชากรแทบไม่ตก (232 → 224)
2. ~~**เป้าหมายกับฉากจบ**~~ — **ทำแล้ว** (`src/sim/reign.ts`)
   เป้าคือผู้ศรัทธา 300 คนและอยู่มาอย่างน้อย 6 ปี คาลิเบรตกับ `npm run sim 20 8` แล้ว:
   เทพเมตตาถึง 2/8 โลก · ปล่อยทิ้ง 0/8 — ยากแต่ทำได้ และวัดฝีมือจริง
   ฉากจบไม่บอกว่า "ชนะ" แต่เล่าว่ารัชสมัยของท่านเป็นแบบไหน เกมเล่นต่อได้
3. **ที่ปรึกษาสองฝ่าย ธรรม/อธรรม** — สองตัวคอยเถียงกันว่าควรทำอะไร
   นี่คือสิ่งที่ให้ "เสียง" กับ B&W และทำหน้าที่เป็นระบบคำใบ้ไปในตัว
   ซึ่งตรงกับเหตุผลที่ตัดขอบเขตรอบที่แล้วพอดี ("ไม่มีอะไรบอกว่าตอนนี้ควรทำอะไร")
4. ~~**สอนโดยการทำให้ดู**~~ — **ทำแล้ว** (`watchMiracle` ใน `creature.ts`)
   ร่ายคาถาใกล้สัตว์ในรัศมี `imitate.watchRadius` แล้วน้ำหนักการกระทำของมันขยับตาม
   คาถาขาวดัน `help`/`worship` ขึ้น คาถาดำดัน `raid` ขึ้น

### ยังอยู่ในคิวทั้งสองทาง

~~**เสียง**~~ — **ทำแล้ว** (`src/core/audio.ts`) สังเคราะห์สดด้วย Web Audio ไม่มีไฟล์เสียงสักไฟล์
คาถาทั้ง 7 มีเสียงคนละบุคลิก บวกเสียงชม/ดุ ยก/วาง ภัยพิบัติ และถึงเป้าหมาย
มีปุ่มเปิด/ปิดบนแถบบน เสียงปลดล็อกตอนแตะจอครั้งแรกตามกติกาของเบราว์เซอร์

**ที่ปรึกษาสองฝ่าย ธรรม/อธรรม** — ยังไม่ได้ทำ สองตัวคอยเถียงกันว่าควรทำอะไร
นี่คือสิ่งที่ให้ "เสียง" กับ B&W และทำหน้าที่เป็นระบบคำใบ้ไปในตัว

**หยิบชาวบ้าน** — หยิบหิน ต้นไม้ อาหาร และยกสัตว์ได้แล้ว เหลือแต่คน
ชาวบ้านใน `villagers3d.ts` ไม่มี state ของตัวเอง ถ้าจะหยิบได้ต้องให้มันมีตัวตนใน `src/sim/` ก่อน
ซึ่งเป็นการตัดสินใจเรื่องขอบเขต ไม่ใช่งานภาพแล้ว ควรคุยก่อนทำ

**หมู่บ้านโตเป็นเมือง** — ตอนนี้กระท่อมแค่เพิ่มจำนวน ยังไม่เปลี่ยนรูปทรงตามขนาด

## ข้อจำกัดที่รู้อยู่แล้ว

- ไม่มีเสียง
- ยังไม่เคยทดสอบบน iPad จริง (ทั้งประสิทธิภาพ WebGL และการหนีบสองนิ้ว)
- สัตว์มีตัวเดียว ไม่มีวิวัฒนาการของประชากรอีกแล้ว (ตัดออกโดยตั้งใจ)
- bundle 600KB เพราะ Three.js (gzip 157KB) โหลดครั้งแรกช้ากว่าเดิม
- ศรัทธาทะลุ `faithCap()` ได้ ~15 หน่วย เพราะการร่ายรำบูชาของสัตว์ใน `creature.ts`
  บวกศรัทธาโดยไม่ clamp (และเป็นเลขวิเศษที่ควรย้ายไป `balance.json`) `npm run sim` เตือนไว้แล้ว

## ข้อความที่ Tap เอาไว้พิมพ์ในเซสชันแรก

```
อ่าน HANDOFF.md กับ CLAUDE.md แล้วสรุปให้ฟังสั้นๆ ว่าเข้าใจโปรเจกต์นี้ว่ายังไง
จากนั้นรอผมบอกว่าจะทำอะไรต่อ
```
