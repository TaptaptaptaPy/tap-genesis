# ปฐมเทพ (genesis)

เกมเทพเจ้าสามมิติ — Black & White เป็นหลัก (ตัดชั้น Spore กับ Civilization ออกแล้ว)
ตอบเป็นภาษาไทย อธิบายโค้ดเป็นขั้นตอน เจ้าของโปรเจกต์ไม่ได้มาสายเขียนโค้ด
พูดตรง ชี้ข้อผิดพลาดได้เลย ไม่ต้องประคอง

**เซสชันแรก อ่าน `HANDOFF.md` ก่อน** (เซสชันถัดไปไม่ต้อง — มันเป็นเอกสารบริบทตั้งต้น ไม่ใช่กติกา)

## คำสั่ง

```bash
npm run dev       # dev server พอร์ต 5173 (Network URL = ที่ iPad เปิด)
npm run visual    # เทสต์ภาพ (Playwright) · visual:update เพื่ออัปเดตภาพอ้างอิง
npm run unit      # เทสต์หน่วย (vitest)
npm run sim       # จำลอง 3 โลก × 20 นาทีเกม + ทดสอบเซฟ/โหลด ใน ~1 วินาที
npm run sim 40 8  # 8 โลก × 40 นาที
npm run check     # tsc --noEmit
```

## กติกาที่ต่างจากค่าเริ่มต้นทั่วไป

**ขอบเขตของเกมถูกตัดลงแล้ว (15 ก.ย. 2026)** เหลือแกน B&W: ศรัทธา → ปาฏิหาริย์ →
สัตว์ที่สอนได้ → หมู่บ้านที่ร้องขอความช่วยเหลือ → แกนธรรม/อธรรม
สิ่งที่ตัดออกและอย่าเอากลับมาโดยไม่คุยก่อน: เทพคู่แข่ง · ประชากรสัตว์ป่าและการผสมพันธุ์ ·
ระบบยุค 5 ยุคกับค่าความรู้ · ฤดูกาล เหตุผลคือผู้เล่นบอกว่า "เล่นไม่รู้เรื่อง"
เพราะมีระบบพร้อมกันหกอย่างโดยไม่มีอะไรบอกว่าตอนนี้ควรทำอะไร

**`src/sim/` ห้าม import อะไรที่แตะ DOM / window / canvas**
นี่คือสิ่งเดียวที่ทำให้ `npm run sim` ทำงานได้ ถ้าต้องส่งข้อความออกไป ให้ push เข้า
`state.log` หรือรับ callback `log()` เข้ามาเป็นพารามิเตอร์
(`src/core/storage.ts` แตะ localStorage จึงอยู่ใน `core/` ไม่ใช่ `sim/`)

**การสุ่มใน `src/sim/` ต้องผ่าน `rng` ที่รับเข้ามาเท่านั้น ห้าม `Math.random()`**
เพราะ seed เดิมต้องให้ผลเดิม ไม่งั้นดีบั๊กสมดุลไม่ได้ และเซฟ/โหลดจะไม่ตรงกัน
(ใน `src/render/` ใช้ได้ เป็นแค่ภาพ)

**ตัวเลขสมดุลอยู่ใน `data/balance.json` เท่านั้น** เจอเลขวิเศษฝังในโค้ดให้ย้ายออกมา

**`npm run sim` เดินโลกเดียวกันสามแบบ** — ปล่อยทิ้ง (ไม่มีเทพ) · เมตตา · พิโรธ
ผู้เล่นจำลองอยู่ใน `src/test/headless.ts` มันตอบคำขอของหมู่บ้านด้วยคาถาที่ตรงกับสิ่งที่ขาด
และสอนสัตว์จริง เดิมเทสต์นี้เรียกแต่ `stepTick` เลยไม่มีอะไรคุมลูปหลักของเกมเลย
**ทุกการตัดสินใจของผู้เล่นจำลองต้องมาจาก state ล้วน ห้ามสุ่มเอง** ไม่งั้นเทสต์เซฟ/โหลดจะเพี้ยน
(ของมันพอร์ตกลับมาจาก snapshot แล้ว rng ตรง แต่ตัวตัดสินใจจะไม่ตรง)

**หลังแก้ `data/balance.json` ให้รัน `npm run sim 20 8`** ห้าอย่างที่ต้องดู:
เมตตาต้องไม่มีโลกล่มเกินครึ่ง · `ธรรม` ของโหมด**ปล่อยทิ้ง**ต้องไม่วิ่งไปติดขอบ ±1 เอง
(โหมดเมตตา/พิโรธติดขอบเป็นเรื่องปกติ เพราะผู้เล่นเลือกข้างสุดตัว) ·
จำนวนหมู่บ้านไม่ทะลุเพดาน · คาถาต้องถูกใช้ครบ 7/7 · ศรัทธาต้องไม่ทะลุ `faithCap()`

**บรรทัด "เมตตา 247 vs ปล่อยทิ้ง 32" คือคำถามว่าเกมนี้มีอยู่ทำไม**
ถ้าวันไหนสองเลขนี้ใกล้กัน แปลว่าการมีเทพไม่ได้เปลี่ยนอะไร ลูปหลักตายแล้ว

**เซฟต้องผ่าน `snapshot()` / `restore()` ใน `src/sim/index.ts` เท่านั้น**
ห้าม `JSON.stringify(state)` ตรงๆ เด็ดขาด — ดูกับดักข้างล่าง

## กับดักที่เคยเสียเวลามาแล้ว

- **`tile.village` กับ `state.villages[]` ชี้ object ก้อนเดียวกัน** JSON ธรรมดาจะทำให้มันแตกเป็น
  สองก้อนแล้วค่าเริ่มไม่ตรงกันเงียบๆ `src/sim/serialize.ts` แก้ด้วยการเก็บเป็น id แล้วผูกกลับ
  และ **ต้องก๊อปลึก** ไม่งั้น "สแนปช็อต" จะเดินหน้าไปพร้อมโลกที่ยังรันอยู่
  `npm run sim` มีเทสต์ข้อนี้อยู่ท้ายผลลัพธ์ ถ้าเห็น "ไม่ใช่ ← พัง" แปลว่าโดนแล้ว
- **แกนธรรม/อธรรมเคยอิ่มตัวที่ −0.98 ทุกโลก** เพราะการบุกของสัตว์สะสมทางเดียว
  แก้ด้วย `align.decayPerTick` — ถ้าเพิ่มการกระทำที่ขยับ align ต้องคิดถึงจุดสมดุลด้วย
- **สัตว์เคยกินความอุดมสมบูรณ์จนหมู่บ้านอดตาย** แก้ด้วย `farmAvoidRadius`
  ทุกอย่างที่กินทรัพยากรบนแผนที่ต้องคิดว่าไปแย่งของหมู่บ้านไหม
- **ตอนเปิดประชากรสัตว์หลายตัว มันรุมบุกหมู่บ้านเดียวจนเหลือ pop 3 พอดีกับ `raidFloorPop` ทุกโลก**
  แก้ด้วย `raidMaxFraction` (เพดานความเสียหายต่อครั้ง) + `raidDefencePerPop/PerEra` (หมู่บ้านสู้กลับ)
  + `wildRaidFromEra` (สัตว์ป่าไม่แตะหมู่บ้านจนกว่าอารยธรรมจะตั้งหลักได้)
- **หมู่บ้านเคยบานเป็น 47 แห่งบนแผนที่ 44×32** เพราะระยะห่างขั้นต่ำเป็นแค่คะแนนลบ ไม่ใช่กฎเหล็ก
  ตอนนี้เพดานอยู่ที่ `maxVillages()` = ผืนดินหารด้วย `tilesPerVillage` และบังคับใน `foundVillage()`
  ทุกทางที่สร้างหมู่บ้าน (แตกหน่อ / คาถา / เทพคู่แข่ง) ถ้าเพิ่มทางใหม่ต้องผ่านฟังก์ชันนี้
- **สัตว์รุ่นก่อตั้งเกิดพร้อมกันหมดเลยตายพร้อมกันหมด** ประชากรคอหักที่อายุ ~220 ticks
  ตอนนี้กระจายอายุตั้งต้นแล้ว ถ้าเพิ่มการเกิดหมู่ต้องกระจายอายุด้วย
- **สีในไฟล์นี้: `--ink` คือสีพื้นหลังเข้ม ไม่ใช่สีตัวอักษร** สีตัวอักษรคือ `--parch`
  เคยเขียน `color: var(--ink)` แล้วตัวหนังสือจมหายไปทั้งแผงมาแล้ว
- **ความสูงของเกาะมาจาก `tile.h` ที่ตัวสร้างโลกผลิตอยู่แล้ว** การขึ้น 3 มิติจึงไม่ต้องแตะ `world.ts` เลย
  ถ้าจะเปลี่ยนความชันของเกาะ แก้ `HEIGHT_SCALE` ใน `world3d.ts` ที่เดียว
- **แสงแรงเกิน + ไม่มี tone mapping = เกาะซีดขาวเมื่อหมุนกล้องไปด้านที่แดดส่องตรง**
  ตอนนี้ใช้ ACESFilmic กับแสงอาทิตย์ 1.45 ถ้าเพิ่มแสงต้องเช็กทุกมุมกล้อง ไม่ใช่มุมเดียว
- **ผิวน้ำต้องเป็น MeshBasicMaterial และห้าม receiveShadow** ไม่งั้นเงาเกาะจะทาบเป็นแผ่นดำใหญ่
  และสีทะเลจะเปลี่ยนตามมุมกล้องจนเห็นเป็นแผ่นสี่เหลี่ยมลอย
- **`looksValid()` ใน `serialize.ts` เคยเช็ก `creatures` (พหูพจน์) ค้างไว้จากตอนตัดขอบเขต**
  พอเหลือ `creature` ตัวเดียว มันเลยตอบ false กับเซฟทุกไฟล์ เกมเซฟลงได้แต่โหลดไม่ขึ้นสักช่อง
  และ `npm run sim` มองไม่เห็นเพราะเรียก `snapshot`/`restore` ตรงๆ ข้ามด่านนี้ไป
  **เทสต์เซฟต้องเดินทางเดียวกับที่ `main.ts` ใช้เสมอ** ไม่ใช่ทางลัด
- **ชาวบ้านใน `villagers3d.ts` ต้องยืนนอก `villageFootprint(v)` เสมอ** กลุ่มกระท่อมโตตามประชากร
  จนกินรัศมีเกือบ 1.7 ตอนหมู่บ้านใหญ่ ถ้าวางคนด้วยเลขคงที่ หมู่บ้านยิ่งโตยิ่งกลืนคนของตัวเองจนหายไปหมด
  ขนาดหมู่บ้านถูกเปิดออกมาจาก `actors3d.ts` เป็นแหล่งเดียว แก้ผังกระท่อมต้องแก้ `HUT_SPREAD` ด้วย
  (ข้อสังเกตที่ยังไม่ได้แก้: `workRadius` มีแค่ 2 แต่หมู่บ้านกินรัศมี ~1.7 — มันเกือบกลืนไร่ตัวเอง)
- **ชาวบ้านใช้ `dt` ไม่ใช่ `performance.now()`** `dt` จาก FixedLoop คูณความเร็วเกมมาแล้ว
  ถ้าใช้เวลานาฬิกาจริง กด 4× โลกจะเดินเร็วขึ้นแต่คนยังเดินเท่าเดิม
- **ชาวบ้านเป็นตัวตนจริงใน `src/sim/folk.ts` แล้ว ไม่ใช่ภาพที่คำนวณจาก `pop`**
  `villagers3d.ts` อ่านตำแหน่งจาก `v.folk` ตรงๆ ห้ามกลับไปเดาตำแหน่งเองด้วยแฮชอีก
  ไม่งั้นคนบนจอจะไม่ใช่คนเดียวกับคนที่มือหยิบขึ้นมา
  จำนวนคนไล่ตาม `pop` แต่ไม่เท่ากันเป๊ะ (`folk.perPop`) และมีเพดานที่ `folk.maxPerVillage`
  **`takeFolk()` / `putFolk()` เป็นทางเดียวที่ย้ายคน** เพราะมันขยับ `pop` ให้ด้วย
- **`z` ของกระสุนใน `physics.ts` เป็นความสูง "สัมบูรณ์" หน่วยเดียวกับ `tile.h` (ราว 0-1.5)**
  ไม่ใช่ความสูงเหนือพื้น และไม่ใช่เมตร เคยเริ่มที่ 0 แล้วเทียบกับ `tile.h` ที่เป็น 0.58
  ผลคือของตกทันทีที่ขว้างทุกครั้ง แล้วดูเหมือนใช้งานได้เพราะมี log ขึ้นว่า "ตกใส่หมู่บ้าน"
  แรงโน้มถ่วงจึงเป็น 3.2 ไม่ใช่ 22 และ `arc` ยืด *เวลาบิน* ไม่ใช่คูณความเร็ว
  (ถ้าไปคูณ vz ของจะไม่ตกตรงเป้า เพราะสมการถูกแก้ไว้ให้ตกพอดีแล้ว)
- **ทดสอบอะไรที่ต้องให้เวลาเดินจากเบราว์เซอร์ ต้องเรียก `stepTick()` เอง**
  แท็บที่ไม่ได้อยู่หน้าสุดโดนเบราว์เซอร์หยุด `requestAnimationFrame` ลูปเกมจะไม่เดินเลย
  แล้วจะดูเหมือนฟิสิกส์ค้าง ทั้งที่มันแค่ไม่ได้ถูกเรียก
- **เพดานศรัทธาหดลงได้เมื่อประชากรลด** ศรัทธาที่สะสมไว้จึงค้างเหนือเพดานได้ถ้าไม่บังคับ
  ตอนนี้ `stepTick()` ตัดที่เพดานให้ท้าย tick **ที่เดียว** ทางอื่นที่บวกศรัทธาไม่ต้อง clamp เอง
  (เคยไล่แก้ผิดจุดมาแล้ว เพราะคิดว่าเป็นเพราะการร่ายรำบูชาบวกเกิน)
- **เสียงต้องปลดล็อกจาก event ของผู้ใช้จริง** `unlockAudio()` ถูกเรียกใน `pointerdown`
  ถ้าเรียกตอนโหลดหน้า เบราว์เซอร์จะบล็อกแล้วเสียงจะเงียบทั้งเกมโดยไม่มี error
- **`setPointerCapture` โยน error ได้กับ pointer บางชนิด** ถ้าไม่ดัก try/catch ไว้
  `pointerdown` จะตายกลางคันและการแตะครั้งนั้นหายไปทั้งครั้ง
- **`setTimeOfDay()` เขียนทับความแรงของแสงทุกเฟรม** ค่า 1.45 ที่ตั้งไว้ตอนสร้างจึงไม่เคยมีผลจริง
  ของเดิมคือ `0.2 + 1.4 * d` พอรวมกับ hemi แล้วความสว่างรวมทะลุ 1 ตอนเที่ยง ACES ไม่ได้พัง
  มันแค่ม้วนส่วนเกินเป็นขาว ทุ่งหญ้าเลยกลายเป็นทุ่งหิมะทั้งเกาะ **ถ้าจะปรับแสง ปรับที่ `setTimeOfDay()`**
- **`THREE.Color(0x......)` แปลง sRGB เป็น linear ให้ตั้งแต่ตอนสร้าง**
  ถ้าเอาค่านั้นเขียนลง `gl_FragColor` ของ ShaderMaterial ตรงๆ สีจะมืดกว่าที่ตั้งไว้มาก
  โดมท้องฟ้าจึงต้องมี `#include <colorspace_fragment>` ปิดท้าย (three แทน include ในเชเดอร์ของเราให้ด้วย)
- **ระนาบผิวน้ำต้องทึบ** ตอนเป็นโปร่งแสง 88% อีก 12% ที่เหลือคือช่องน้ำในตารางพื้นดิน
  ซึ่งมีแค่ในกรอบ 24×24 ของโลก นอกกรอบไม่มี ทะเลเลยมีสองเฉดแบ่งด้วยเส้นตรงเป็นสี่เหลี่ยมรอบเกาะ
  ทางแก้ที่ **ไม่ได้ผล**: ดันขอบตารางพื้นดินออกไป 240 หน่วย สามเหลี่ยมยักษ์จะลากสีและ normal เพี้ยนทั้งผืน
- **หมอกต้องเริ่มหลังเกาะ ไม่ใช่กลางเกาะ** กล้องปกติอยู่ที่ระยะ 26 เกาะกว้าง 24
  ของเดิมหมอกเริ่มที่ 40 จบ 110 ขอบเกาะฝั่งไกลเลยจางไปตลอดเวลา ตอนนี้ 70/175
  และสีหมอกต้องเป็นสีขอบฟ้า ไม่ใช่สีบนหัว ไม่งั้นเกาะจะจางไปคนละสีกับฟ้าที่อยู่หลังมัน
- **โมเดล glTF ของ Quaternius ออกมาเกือบดำถ้าไม่แปลงวัสดุ** มันแปลงมาจาก FBX/Phong
  (`extras.fromFBX.isTruePBR: false`) แต่มี `metallicFactor` ติดมา 0.4 ผิวโลหะต้องมี
  environment map ถึงจะมีอะไรสะท้อน ฉากนี้ไม่มี สีเลยหายไป 40%
  ใช้ `flattenToLambert()` ใน `gltf.ts` เสมอ — ทั้งเกาะใช้ Lambert อยู่แล้ว
  ถ้าปล่อยให้สัตว์เป็น PBR ตัวเดียว แสงจะไม่เข้ากับอย่างอื่นบนจอ
- **ท่าในไฟล์ของ Quaternius มาสองชื่อต่อหนึ่งท่า** "Walk" กับ "AnimalArmature|Walk"
  `loadRigged()` ตัดชื่อยาวทิ้งให้แล้ว ตารางใน `data/models.json` ใช้ชื่อสั้นเสมอ
- **ทุกชุดโมเดลใช้หน่วยไม่เหมือนกัน** วัวตัวนี้ยาว 8 หน่วยในไฟล์ ต้องผ่าน `normalise()`
  ให้เหลือด้านยาวสุด 1 หน่วยและเท้าอยู่ที่ y=0 ก่อนเสมอ ไม่งั้นเปลี่ยนโมเดลทีหลังต้องจูนเลขใหม่หมด
- **ภาพเกาะทั้งใบจับการเปลี่ยนแปลงของสัตว์ไม่ได้** สัตว์กินพื้นที่ราว 2,400 พิกเซล
  แต่เกณฑ์ความต่างที่ยอมให้คือ 1.2% ของ 1000×720 = 8,640 พิกเซล
  เปลี่ยนโมเดลทั้งตัวแล้วเทสต์ยังขึ้นเขียวมาแล้ว **ของเล็กต้องมีภาพระยะใกล้ของตัวเอง**
- **`data/models.json` เป็นของชั้นภาพ `src/sim/` ห้ามอ่าน** ชื่อไฟล์กับชื่อท่าไม่ใช่ตัวเลขสมดุล
  จึงไม่อยู่ใน `balance.json`
- **ลบโฟลเดอร์ภาพอ้างอิงทิ้งทุกครั้งที่เปลี่ยนไฟล์ asset** `--update-snapshots` เขียนทับ
  เฉพาะภาพที่ต่างเกินเกณฑ์ ภาพที่เปลี่ยนน้อยกว่านั้นจะค้างของเก่าไว้เงียบๆ
  เคยหลงว่าการทับสีไม่ทำงาน ทั้งที่มันทำงานแล้ว แต่ไปอ่านภาพเก่า
- **โมเดลของ Kenney อ้างเท็กซ์เจอร์เป็นไฟล์แยก** GLTFLoader จะขึ้น
  "Couldn't load texture" ใน console แล้วโมเดลออกมาขาวโพลน
  แพ็กใหม่ผ่าน `tools/gltf_report.py` ให้ฝังเท็กซ์เจอร์เข้าไฟล์เดียวเสมอ
- **ชาวบ้านไม่ใช่ InstancedMesh อีกแล้ว** หนึ่งคนหนึ่งร่างเพราะแต่ละคนเล่นคนละท่า
  วัดแล้วที่ 96 คน: แบบเดิม 34.6 ms แบบใหม่ 41.1 ms ต่อเฟรม (ช้าลง 18%)
  `npm run visual -- tests/perf.spec.ts` วัดซ้ำได้ทุกเมื่อ และล้มถ้าเกินสองเท่า
  (ตัวเลขดิบช้ากว่าเครื่องจริงมาก เพราะ Playwright เรนเดอร์ด้วยซอฟต์แวร์ — ใช้เทียบอัตราส่วนเท่านั้น)
- **`stepFolk()` ตัดจำนวนชาวบ้านกลับเท่าที่ประชากรรองรับทุก tick** ถ้ายัดคนเข้าไปเองเพื่อทดสอบ
  แล้วเดินเวลาต่อ คนที่ยัดเข้าไปจะหายเกลี้ยงภายในหนึ่งจังหวะ
- **ผิวน้ำเป็นเชเดอร์แล้ว อยู่ใน `src/render/water.ts`** ความลึกมาจากพื้นผิว `depthMap`
  ที่สร้างจากความสูงของช่อง (160×160) สร้างใหม่เฉพาะตอน `terrainVersion` เปลี่ยน ไม่ใช่ทุกเฟรม
  เชเดอร์จึงรู้ว่าจุดที่วาดอยู่ห่างฝั่งแค่ไหน แล้วเอาไปทำฟองคลื่น
- **คลื่นความถี่คงที่บนระนาบน้ำจะกลายเป็นลายตารางเต็มทะเล** ระนาบกว้างเป็นร้อยเท่าของเกาะ
  sin(x*1.35) จึงซ้ำเป็นสิบๆ รอบในภาพเดียว ต้องคูณด้วย `nearShore` ให้มีผลเฉพาะน้ำตื้น
  ทะเลเปิดใช้คลื่นความถี่ต่ำมากแทน (0.055) แค่ให้ไม่ตายสนิท
- **`UniformsUtils.merge()` โคลน uniform ทุกตัว** ยัด texture เข้าไปก่อน merge จะได้ null ค้าง
  ต้องตั้งค่า `uniforms.depthMap.value` หลัง merge เสมอ
- **เมฆบนโดมท้องฟ้ามองไม่เห็นด้วยมุมกล้องปกติ** กล้องก้มลงราว 53 องศา ครึ่งบนของเฟรม
  ยังอยู่ "ใต้" เส้นขอบฟ้า ครึ่งที่ผู้เล่นเห็นจริงคือ**เงาเมฆที่พาดบนพื้น**
  ทั้งสองใช้พื้นผิวและออฟเซ็ตก้อนเดียวกันใน `clouds.ts` เงาจึงตรงกับก้อนที่เห็นเสมอ
- **`onBeforeCompile` มีช่องเดียวต่อวัสดุ** ใครมาทีหลังเขียนทับของคนแรกเงียบๆ
  ต้นไม้ต้องมีทั้งเงาเมฆและลม จึงต้องต่อผ่าน `patchMaterial()` ห้ามตั้งตรงๆ
- **สเกลเมฆ 0.012 ทำให้ทั้งเกาะอยู่ในเมฆก้อนเดียว** อ่านออกมาเป็น "แสงไม่สม่ำเสมอ"
  ไม่ใช่ "เงาเมฆ" เกาะกว้าง 24 หน่วย สเกลที่ใช้ได้จริงคือราว 0.055
- **สีหญ้าต้องใกล้สีทุ่งมาก** ถ้าเข้มกว่า จากระยะกล้องปกติมันจะอ่านเป็นเศษดินกระจายเต็มเกาะ
  หญ้ามีหน้าที่เพิ่มผิวสัมผัสตอนซูมเข้าใกล้ ไม่ใช่เพิ่มจุดด่างตอนมองทั้งเกาะ
- **ตรวจฉาก 3 มิติจาก console ได้** ตอน dev มี `window.__genesis` = { game, world, villages, creature, terrain(), state() }

## แผนที่ไฟล์

```
src/sim/      ตรรกะเกมล้วน ไม่มี DOM — world, village, folk (ชาวบ้านรายคน), creature,
              miracle, disaster, reign, advisor, physics (หยิบ/ขว้าง + วิถีโค้ง), serialize
src/render/   world3d.ts (ฉาก แสง กล้องโคจร raycast) · terrain3d.ts (เกาะ mesh + ต้นไม้ + ทะเล)
              actors3d.ts (หมู่บ้าน + สัตว์) · villagers3d.ts (ชาวบ้านที่เดินได้)
              fx3d.ts (อนุภาค + วงเป้า) · hand3d.ts (มือของพระเจ้า + ของที่ลอยอยู่)  [Three.js]
src/core/     loop.ts (fixed timestep) · rng.ts (mulberry32 + state) · storage.ts (localStorage)
              audio.ts (เสียงสังเคราะห์สด ไม่มีไฟล์เสียง)
src/ui/       hud.ts — แถบบน ปุ่มคาถา แผงสัตว์ แผงตรวจสอบช่อง และ "แถบบอกว่าควรทำอะไร"
src/test/     headless.ts — `npm run sim`
```
