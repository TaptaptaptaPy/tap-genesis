{
  "name": "genesis",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "เกมเทพเจ้า/วิวัฒนาการ — Black & White x Spore x Civilization",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host",
    "sim": "tsx src/test/headless.ts",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^26.5.1",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vite": "^7.0.0"
  }
}
